# ========================================
# PROTEÇÃO DO DIRETÓRIO scripts/
# Bloqueia TODO acesso web a scripts de manutenção.
# A atualização do banco é automática via db_connect.php;
# estes scripts existem apenas para uso interno/CLI.
# ========================================

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order Allow,Deny
    Deny from all
</IfModule>
